# Core server
fastapi==0.135.3
uvicorn[standard]==0.44.0
python-multipart>=0.0.20

# PyTorch (installed separately via install.py with CUDA index)
# DO NOT add torch/torchaudio here — they need --index-url

# OmniVoice deps (omnivoice itself installed --no-deps via install.py)
transformers==5.3.0
accelerate>=1.13.0
safetensors>=0.7.0
tokenizers>=0.22.0
sentencepiece>=0.2.0
soundfile>=0.13.0
pydub>=0.25.0
audioop-lts>=0.2.0
webdataset>=1.0.0
numpy>=2.0.0

# HuggingFace model download
huggingface_hub==1.9.0
